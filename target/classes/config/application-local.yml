spring:
  datasource:
    username: sa
    url: jdbc:h2:mem:quasardb
    driverClassName: org.h2.Driver
    password: ''
  h2:
    console:
      enabled: 'true'
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
logging:
  level:
    org:
      springframework: INFO
    root: INFO
    com:
      example:
        quasar: 'DEBUG  '
